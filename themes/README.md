# Themes

Keep shared visual presets in this directory. A theme should be a reusable module
or data file imported by the application configuration that consumes it.

System-level nix-darwin settings do not provide a universal theme option. Add
application themes here when their application is configured, or add Home
Manager later for declarative terminal, editor, and shell theming.
